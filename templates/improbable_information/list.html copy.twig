{% extends 'base.html.twig' %}

{% block title %}Liste des infos impro'{% endblock %}

{% block body %}

    <div class="container">

        
        <div class="title">
            <h1>Liste des infos impro' (mais cool à savoir)</h1>
        </div>
        {% for information in informations %}
            <div class="container-improbable-information-list">
                <div class="improbable-information-container">
                    <p> {{ information.description }} </hp>
                </div>
            </div>
        {% endfor %}
        {{ knp_pagination_render(informations) }}
    </div>
{% endblock %}
